name: Sync to Feishu

on:
  push:
    paths:
      - 'docs/**.md'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v42
        with:
          files: docs/**.md
      
      - name: Get Feishu Token
        id: token
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          response=$(curl -s -X POST \
            -H "Content-Type: application/json" \
            -d "{\"app_id\":\"${{ secrets.FEISHU_APP_ID }}\",\"app_secret\":\"${{ secrets.FEISHU_APP_SECRET }}\"}" \
            https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal)
          token=$(echo "$response" | jq -r '.tenant_access_token')
          echo "token=$token" >> $GITHUB_OUTPUT
          echo "Token got."
      
      - name: Send to Feishu
        if: steps.changed-files.outputs.any_changed == 'true'
        env:
          FEISHU_TOKEN: ${{ steps.token.outputs.token }}
          FEISHU_RECEIVE_ID: ${{ secrets.FEISHU_RECEIVE_ID }}
          REPO_URL: https://github.com/${{ github.repository }}/blob/main
        run: |
          for file in ${{ steps.changed-files.outputs.all_changed_files }}; do
            filename=$(basename "$file")
            
            # 读取文件内容 (前 10 行)
            raw_content=$(cat "$file" | head -10)
            
            # 使用 jq 安全地构建整个 JSON payload
            # --arg 会自动处理所有特殊字符转义
            payload=$(jq -n \
              --arg receive_id "$FEISHU_RECEIVE_ID" \
              --arg filename "$filename" \
              --arg content "$raw_content" \
              --arg url "$REPO_URL/$file" \
              '{
                "receive_id": $receive_id,
                "msg_type": "interactive",
                "content": {
                  "card": {
                    "header": {
                      "title": {
                        "tag": "plain_text",
                        "content": ("📚 " + $filename)
                      },
                      "template": "blue"
                    },
                    "elements": [
                      {
                        "tag": "div",
                        "text": {
                          "tag": "lark_md",
                          "content": $content
                        }
                      },
                      {
                        "tag": "action",
                        "actions": [
                          {
                            "tag": "button",
                            "text": {
                              "tag": "plain_text",
                              "content": "查看原文"
                            },
                            "url": $url,
                            "type": "primary"
                          }
                        ]
                      }
                    ]
                  }
                }
              }')
            
            echo "Sending: $filename"
            
            # 发送请求
            # 注意：URL 末尾显式添加了 ?receive_id_type=open_id
            response=$(curl -s -w "\n%{http_code}" -X POST \
              -H "Authorization: Bearer $FEISHU_TOKEN" \
              -H "Content-Type: application/json" \
              -d "$payload" \
              "https://open.feishu.cn/open-apis/im/v1/messages?receive_id_type=open_id")
            
            http_code=$(echo "$response" | tail -n1)
            body=$(echo "$response" | sed '$d')
            
            if [ "$http_code" == "200" ]; then
              code=$(echo "$body" | jq -r '.code')
              if [ "$code" == "0" ]; then
                echo "✅ Success! Message sent to Feishu."
              else
                echo "❌ Feishu API Error: $body"
                exit 1
              fi
            else
              echo "❌ HTTP Request Failed: $http_code"
              echo "Response: $body"
              exit 1
            fi
          done
